#! /usr/bin/env Python3

main():
    print("Input string: ")
    inputString = input()
    while !isinstance(inputString, str):
        print("Try again!")
        print(Input string: ")
        inputString = input()
    stringLength = len(inputString)
    
    What I want to do: Separate characters into array of tuples (triples?), with (char, rand) (char, initpos, rand)?
    Then sort array based on rand
    output = inputString + "\n"
    while i < stringLength:
        
